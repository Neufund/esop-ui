import getMuiTheme from 'material-ui/styles/getMuiTheme';
// color are taken from _colors.scss files
export default getMuiTheme({
    palette: {
        primary1Color: "#686e75", //this one is just taken randomly
        textColor: "#424a52",
        accent1Color: "#d5e20f"
    }
});